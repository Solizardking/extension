---
name: grok-paper
description: Ask Clawd Grok for a paper perps recommendation using live Phoenix reads.
---

# Grok paper

1. Confirm `XAI_API_KEY` is set (name only).
2. `clawd-grok --prompt "<intent>"` or `node $CORE_AI_ROOT/clawd-grok/dist/index.js --prompt "..."`.
3. Keep the answer in paper mode. Include signal vs observed data.
4. Do not start `clawd-grok mcp` live trade groups unless requested.
