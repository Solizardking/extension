---
name: phantom
description: Build Phantom-connected Solana frontends with Connect SDKs and Helius Sender/DAS. Use when the user mentions Phantom wallet or Connect SDK.
---

# Phantom

Use `@phantom/react-sdk` / `browser-sdk` / `react-native-sdk`. Portal App ID for OAuth. Never embed `HELIUS_API_KEY` in the client. Deep refs: `$CORE_AI_ROOT/helius-cursor/skills/phantom/` and `$CORE_AI_ROOT/helius-skills/helius-phantom/SKILL.md`.
