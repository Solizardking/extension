---
name: pump-mcp
description: Use the in-tree Pump Fun MCP (core-ai/mcp-server) for quotes, instruction builders, fees, AMM, and session wallets. Use when launching or analyzing pump.fun tokens.
---

# Pump MCP

`$CORE_AI_ROOT/mcp-server/dist/index.js` (`pump-mcp`). 55 tools. Builders return instructions — a wallet must sign. Quote before build. Resources: `solana://programs`, `solana://config`. Same family as Cheshire `mcp-server`.
