---
name: dflow
description: Build Solana trading apps with DFlow spot, prediction markets, Proof KYC, and Helius Sender. Use when the user mentions DFlow or pond.dflow.net.
---

# DFlow

MCP: `https://pond.dflow.net/mcp`. Proxy Trade API through a backend (no CORS). Atomic amounts. Poll `/order-status` for async. Submit via Sender.

Deep refs: `$CORE_AI_ROOT/helius-cursor/skills/dflow/` and `$CORE_AI_ROOT/helius-skills/helius-dflow/SKILL.md`.
