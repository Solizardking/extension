---
name: arena
description: Mint or inspect Clawd Code Agent Arena identity (Metaplex Core / svm://).
---

# Arena

1. Prefer `clawd-code arena status` first.
2. Mint/register only with operator wallet consent. Never paste private keys.
3. Save `assetAddress` and `globalId` (`svm://solana-mainnet/<asset>`) to `~/.clawd-code/arena-identity.json` without echoing secrets.
4. Reviews require an on-chain `$CLAWD` payment `txSignature`.
