---
name: svm
description: Explain Solana protocol internals (accounts, SVM, consensus, tokens, validators) using Helius knowledge tools and public SIMDs. Use when the user asks how Solana works, not how to call an API.
---

# SVM

This skill is architecture, not app scaffolding. Prefer Helius knowledge tools (`searchSolanaDocs`, `fetchHeliusBlog`, `getSIMD`, `readSolanaSourceFile`) when they are connected.

Cite blog URL, SIMD number, or Agave/Firedancer path in substantive answers. Deep refs: `$CLAWD_CODE_ROOT/clawd-plugin/skills/svm/references/`.
