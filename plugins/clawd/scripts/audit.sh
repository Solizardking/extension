#!/usr/bin/env bash
set -euo pipefail
cat >/dev/null || true
echo "[clawd] session audit: six-law harness still binds. Prefer paper OODA, never commit keypairs, and keep Cheshire/0x keys server-side."
exit 0
