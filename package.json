{
  "name": "clawd-plugin-template",
  "private": true,
  "description": "Cursor plugin marketplace for Clawd. The VS Code extension lives in extension/.",
  "scripts": {
    "validate": "node scripts/validate-template.mjs",
    "package": "npm run package --prefix extension",
    "publish:vsce": "npm run publish --prefix extension"
  }
}
